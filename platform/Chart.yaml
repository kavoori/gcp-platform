apiVersion: v2
name: platform
description: What every application on the cluster shares, rendered from one environment name and one project. The platform namespace, the one Gateway, and how the Config Connector add-on behaves.
type: application
version: 0.0.0
