# One Pub/Sub topic, the smallest Google resource a chart will create. Applied by Argo CD,
# turned into a real topic by Config Connector acting as dev-config-connector, and deleted from
# Google again when this object goes.
apiVersion: pubsub.cnrm.cloud.google.com/v1beta1
kind: PubSubTopic
metadata:
  name: config-connector-proof
  namespace: config-connector-proof
spec: {}
